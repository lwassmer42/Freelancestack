{
  "name": "clientdrop",
  "version": "1.0.0",
  "description": "Client portal SaaS for freelancers — Cloudflare Workers backend",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute clientdrop-db --file=schema.sql"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
